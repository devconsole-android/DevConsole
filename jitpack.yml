# JitPack's own default command already excludes tests, lint, and signing; only the JDK is pinned
# here, so its defaults keep applying. AGP 8.13 and jvmToolchain(17) both need 17.
jdk:
  - openjdk17
